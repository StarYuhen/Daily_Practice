import fse from "fs-extra";
export declare function createTestHTML({ destFold, outputFile, }: {
    destFold: string;
    outputFile: (file: string, data: any, options?: string | fse.WriteFileOptions | undefined) => Promise<void>;
}): Promise<void>;
